I ned to create a small drive application that will be in nextjs frontend and backend both and using mongo db or mongoose whatever works with nextjs

mobile first appliocation is: an user will signup/login using email no auth verification, then they can create folders/subfolders similar to google drive and then they can create a File

definition of File in our case, A file is basically a exam that they can give, user can create quiestions in the application for the file questions types are, choice, single select, ytes no, mcqs, textarea, generatlly used in the exam, then once the file is saved that can be shared with any other user by putting their email into that box, that new user can take a test and that will be saved under the same file segregrated by the user who saved it, later anyone who created the file and one who took the test both can view the result, they can always take the same test again if required, while creating the question user can also add answer to that question so that next person can immediately see the result, I do not think its possible to see results for text based questions but for that user who created the file can manually add the result 

now in the dashboard, user can see recent files and then their drive with folders they created and one static folder called Shared with me, and home, home is the default folder that we will give but user can create more folder and subfolder if required

that is the total app


